/**
 * Interface for the socket user.
 *
 * @export
 * @interface SocketUser
 * @property {string} userId - The user ID.
 * @property {string} socketId - The socket ID.
 */
export interface SocketUser {
  userId: string;
  socketId: string;
}
